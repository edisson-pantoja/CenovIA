[build]
builder = "NIXPACKS"

[build.env]
NODE_VERSION = "22"

[deploy]
startCommand = "npm install --legacy-peer-deps && npm run start --workspace=packages/backend"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
